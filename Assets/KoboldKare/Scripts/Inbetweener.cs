using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inbetweener {
    /*AbstractGoTween _tween;
    public float distanceTrigger = 0.2f;
    private float realData = 0;
    public float data {
        set {
            if (Mathf.Abs(value-realData) > distanceTrigger ) {
                _tween = Go.to(realData, 0.25f, new GoTweenConfig().floatProp(value).setEaseType(GoEaseType.BounceOut));
                _tween.play();
            }
        }
        get {
        }
    }*/
}
